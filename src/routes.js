const express = require("express");
// Example controller import
//const StatusesController = require("./controllers/StatusesController");

const routes = express.Router();

// Example routes, controllers e methods
//routes.post("/status", StatusesController.store);
//routes.get("/status", StatusesController.show);
//
//routes.get("/status/:id", StatusesController.index);
//routes.put("/status/:id", StatusesController.update);
//routes.delete("/status/:id", StatusesController.delete);

module.exports = routes;